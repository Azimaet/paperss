
<div id="layout" 
    data-template="{{ template }}" 
    data-username="{{ user ? user.username : null }}"
    {% if template == "Register" or template == "BoardForm" %}
    data-token="{{ token|json_encode }}"
    {% endif %}>
    <div id="app">
        <app></app>
    </div>
</div>